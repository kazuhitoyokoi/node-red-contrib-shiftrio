node-red-contrib-shiftrio
=====================

A node to connect to shiftrio.io via MQTT over WebSocket

## Install

Run the following command in your Node-RED user directory - typically `~/.node-red`

        npm install node-red-contrib-shiftrio

